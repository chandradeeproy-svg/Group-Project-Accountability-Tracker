// =============================================================
// [DEAD CODE] This entire component is unused.
// It is never imported or routed in App.jsx.
// Superseded by GroupDetails.jsx which handles /groups/:groupId
// Kept for reference — safe to delete when ready.
// =============================================================

// import { useParams } from "react-router-dom";
// export default function ProjectPage() {
//     const { projectId } = useParams();
//     return (<div>
//       <h1>Project Page</h1>
//       <p>Project ID: {projectId}</p>
//     </div>);
// }

// Minimal default export to prevent any import errors
export default function ProjectPage() { return null; }
